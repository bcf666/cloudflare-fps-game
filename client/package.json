{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && esbuild ../server/src/index.ts --bundle --format=esm --outfile=dist/_worker.js --external:cloudflare:* --tsconfig=../server/tsconfig.json",
    "preview": "vite preview"
  },
  "dependencies": {
    "@babylonjs/core": "^7.31.0",
    "@babylonjs/havok": "^1.3.7"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
